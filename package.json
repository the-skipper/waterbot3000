{
    "name": "waterbot3000",
    "version": "0.1.0",

    "scripts": {
        "start": "yarn --cwd server install && yarn --cwd server start ",
        "postinstall": "yarn --cwd client install && yarn --cwd client build"
    },
    "engines": {
        "yarn": "1.13.0"
      }
  }
  